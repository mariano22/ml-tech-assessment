[tool.poetry]
name = "ml-tech-assessment"
version = "0.1.0"
description = "Transcript Analysis API using OpenAI"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
openai = "^1.31.0"
pydantic = "^2.5.3"
pydantic-settings = "^2.1.0"
fastapi = "^0.109.0"
uvicorn = "^0.24.0"
httpx = "^0.26.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.4"
pytest-cov = "^4.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
